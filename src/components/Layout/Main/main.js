import { Fragment } from "react";
import classes from "./main.module.css";

const Main = () => {
	return (
		<Fragment>
		<main className={classes.home}>Courses here</main>
		</Fragment>
	);
};

export default Main;
